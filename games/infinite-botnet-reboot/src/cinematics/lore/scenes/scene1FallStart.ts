import type { LoreSceneDefinition } from '../types';

export const loreSceneFallStart: LoreSceneDefinition = {
  id: 'fall-start',
  i18nKey: 'reboot.overlay.lore.scene1',
  toneClass: 'lore-scene-fall-start',
  hasCanvasLayer: true,
};
